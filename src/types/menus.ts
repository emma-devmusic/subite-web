export interface SidebarMenuItem {
    forAdmin: boolean;
    text: string;
    link: string;
    icon: string;
}
export interface MenuItem {
    isBoth: boolean;
    isLogged: boolean;
    text: string;
    link: string;
    icon: string;
}